import { styled } from 'styles/stitches.config'

export const LoaderWrapper = styled('div', {
  width: '100%',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  margin: '2.5rem 0',
})
